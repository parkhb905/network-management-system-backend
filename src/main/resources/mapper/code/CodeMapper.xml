<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.network.nms.mapper.code.CodeMapper">

    <select id="findCodesByGroupCode" resultType="com.network.nms.dto.code.CodeResponse">
        SELECT
            cd.code_id
            , cd.code
            , cd.code_name
        FROM
            code cd
        INNER JOIN
            code_group grp
        ON
            grp.group_id = cd.group_id
        WHERE
            cd.deleted = FALSE
        AND
            grp.deleted = FALSE
        AND
            grp.group_code = #{groupCode}
        ORDER BY
            cd.code ASC
    </select>

</mapper>