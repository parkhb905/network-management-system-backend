<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.network.nms.mapper.util.SystemResourceMapper">

    <insert id="insertSystemResource">
        INSERT INTO system_resource (
            device_id
            , cpu_usage
            , memory_usage
        ) VALUES (
            #{deviceId}
            , #{cpuUsage}
            , #{memoryUsage}
        )
    </insert>

</mapper>